{
  "name": "cypress11",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pretest": "rimraf -r ./cypress/results/json/*.json",
    "script":"cypress run",
    "test":"cypress run || generate-mochawesome-report",
    "posttest": "generate-mochawesome-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^6.5.0",
    "cypress-downloadfile": "^1.2.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.2"
  },
  "devDependencies": {
    "cypress-file-upload": "^5.0.2",
    "cypress-mochawesome-reporter": "^1.3.0"
  }
}
